/**
 * Scenes-stack math helpers.
 *
 * Re-exported from the canonical `utils/math.ts` (single source of truth) to
 * remove the historical duplicate implementations. New code should import from
 * `@app/utils/math` directly. See architecture-standards.md#math.
 */

export { lerp, smooth01 } from './math';
