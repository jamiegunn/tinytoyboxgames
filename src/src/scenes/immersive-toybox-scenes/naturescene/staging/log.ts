import { Vector3 } from 'three';
import type { EntityPlacement } from '../types';

/** Casts the single fallen log with its hollow and raccoon. */
export const LOG_STAGING: EntityPlacement = { position: new Vector3(-3.1, 0, 0) };
