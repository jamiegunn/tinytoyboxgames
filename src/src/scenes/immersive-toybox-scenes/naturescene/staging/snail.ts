import { Vector3 } from 'three';
import type { EntityPlacement } from '../types';

/** Casts the single snail with its idle crawl path. */
export const SNAIL_STAGING: EntityPlacement = { position: new Vector3(3.5, 0.04, 3) };
