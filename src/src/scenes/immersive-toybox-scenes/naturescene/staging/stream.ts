import { Vector3 } from 'three';
import type { EntityPlacement } from '../types';

/** Casts the single stream running through the scene center. */
export const STREAM_STAGING: EntityPlacement = { position: new Vector3(0, 0, 0) };
