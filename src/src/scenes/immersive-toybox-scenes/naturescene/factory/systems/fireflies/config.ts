import { Color } from 'three';
import type { FireflyConfig } from './types';

export const FIREFLY_CONFIG: FireflyConfig = {
  count: 14,
  glowColor: new Color(0.95, 0.9, 0.3),
  dimColor: new Color(0.15, 0.13, 0.02),
  lightColor: new Color(0.9, 0.85, 0.35),
};
