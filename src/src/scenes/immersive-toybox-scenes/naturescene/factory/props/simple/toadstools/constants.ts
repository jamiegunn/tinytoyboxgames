import { Color } from 'three';

// ── Stem ──
export const STEM_TOP_RADIUS = 0.015;
export const STEM_BOTTOM_RADIUS = 0.02;
export const STEM_HEIGHT = 0.08;
export const STEM_Y = 0.04;
export const STEM_COLOR = new Color(0.92, 0.88, 0.78);

// ── Cap ──
export const CAP_RADIUS = 0.04;
export const CAP_Y = 0.08;
export const CAP_COLOR_LIGHT = new Color(0.85, 0.75, 0.55);
export const CAP_COLOR_DARK = new Color(0.7, 0.6, 0.4);
export const CAP_COLOR_THRESHOLD = 0.5;
