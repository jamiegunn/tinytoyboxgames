import { Color } from 'three';

// ── Nut ──
export const NUT_RADIUS = 0.055;
export const NUT_SCALE_Y = 1.2;
export const NUT_COLOR = new Color(0.55, 0.35, 0.15);

// ── Cap ──
export const CAP_RADIUS = 0.06;
export const CAP_Y = 0.02;
export const CAP_COLOR = new Color(0.4, 0.25, 0.1);

// ── Stem ──
export const STEM_RADIUS = 0.005;
export const STEM_HEIGHT = 0.03;
export const STEM_Y = 0.055;
