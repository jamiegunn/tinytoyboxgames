/**
 * Dimensions and tuning values for the generated simple prop example.
 *
 * Pulling even small authored values into `constants.ts` is deliberate. The
 * template wants future scenes to normalize this habit early so larger props do
 * not end up with magic numbers scattered through mesh creation code.
 */

export const BASE_RADIUS = 0.42;
export const BASE_HEIGHT = 0.22;
export const ACCENT_RADIUS = 0.15;
export const ACCENT_HEIGHT = 0.26;
export const SIDE_TOKEN_COUNT = 3;
export const SIDE_TOKEN_RADIUS = 0.09;
export const SIDE_TOKEN_DISTANCE = 0.34;
