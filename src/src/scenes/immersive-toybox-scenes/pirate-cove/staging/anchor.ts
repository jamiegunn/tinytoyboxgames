/**
 * Placement data for the anchor prop leaning against the railing.
 */

import { Vector3 } from 'three';
import type { EntityPlacement } from '../types';

/** Authored placement for the anchor. */
export const ANCHOR_STAGING: ReadonlyArray<EntityPlacement> = [{ position: new Vector3(-4.5, 0, 3.5), rotY: Math.PI * 0.95, scale: 1 }];
