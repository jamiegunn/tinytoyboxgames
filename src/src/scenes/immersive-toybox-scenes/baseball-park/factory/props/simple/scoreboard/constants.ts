/**
 * Dimensions for the outfield scoreboard.
 *
 * A board on two posts with a little roof and three coloured counting tokens.
 * Deliberately no digits or letters: the age floor bars reading from the core
 * experience, so the board keeps score the way a three-year-old does — with
 * colours.
 */

/** Post radius, height, and how far apart the two posts stand. */
export const POST_RADIUS = 0.07;
export const POST_HEIGHT = 1.6;
export const POST_SPACING = 1.5;

/** Board panel dimensions, centred above the posts. */
export const BOARD_WIDTH = 1.9;
export const BOARD_HEIGHT = 1.1;
export const BOARD_THICKNESS = 0.1;
export const BOARD_CENTER_Y = 1.85;

/** Inset face panel and the tokens on it. */
export const FACE_WIDTH = 1.7;
export const FACE_HEIGHT = 0.9;
export const FACE_THICKNESS = 0.02;
export const TOKEN_RADIUS = 0.12;
export const TOKEN_SPACING = 0.5;

/** Roof slab overhanging the board. */
export const ROOF_WIDTH = 2.0;
export const ROOF_HEIGHT = 0.08;
export const ROOF_DEPTH = 0.3;
